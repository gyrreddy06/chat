import React from 'react';
import { View, Text } from 'react-native';

// This is a placeholder for the compose tab that redirects to chat/new
// The actual navigation happens in the tab press handler in (tabs)/_layout.tsx
export default function ComposeScreen() {
  return (
    <View>
      <Text>Redirecting...</Text>
    </View>
  );
}